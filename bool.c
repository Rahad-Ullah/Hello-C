#include<stdio.h>
#include<stdbool.h>  // this is for bool data type
int main(){
    bool a = true;
    printf("%d", a);    // boolean doesn't have any format specifier. it will print 1 if true and 0 if false
} 